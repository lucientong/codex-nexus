server:
  host: 0.0.0.0
  port: 8000
  debug: true

database:
  host: localhost
  port: 3306
  user: root
  password: your_password
  database: codex_nexus
  charset: utf8mb4

redis:
  host: localhost
  port: 6379
  db: 0
  password: null

jwt:
  secret_key: your_secret_key
  algorithm: HS256
  access_token_expire_minutes: 1440  # 24 hours

cos:
  secret_id: your_secret_id
  secret_key: your_secret_key
  region: ap-guangzhou
  bucket: your_bucket_name

wechat:
  app_id: your_app_id
  app_secret: your_app_secret 